{
  "name": "workspace",
  "version": "1.0.0",
  "description": "A Node.js Telegram bot that automatically welcomes new group members with customizable messages and inline URL buttons. The bot automatically deletes welcome messages after a specified time to keep groups clean.",
  "main": "bot.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1",
    "node-telegram-bot-api": "^0.66.0"
  }
}
